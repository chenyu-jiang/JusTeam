import React from 'react';
import LoginForm from '../forms/LoginForm'

const LoginPage=()=>(
    <div>
        Login page with a
        <LoginForm/>
    </div>

);

export  default  LoginPage;